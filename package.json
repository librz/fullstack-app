{
	"name": "fullstack-app",
	"version": "1.0.0",
	"description": "A monorepo full stack project",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"dev:server": "npm run dev -w apps/server",
		"dev:client": "npm run dev -w apps/client"
	}
}